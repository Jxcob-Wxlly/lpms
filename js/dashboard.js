// dashboard.js - placeholder for dashboard widgets/summary stats
document.addEventListener("DOMContentLoaded", () => {
  console.log("LPMS dashboard loaded.");
  // Summary card values are static in this prototype.
  // Once the backend is connected, these will be populated from
  // the Session, School, and Learner tables.
});
